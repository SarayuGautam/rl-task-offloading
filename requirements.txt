# Exact versions used to produce every result in results/ (Python 3.11.15).
# The pre-fix artifacts were also reproduced bit-for-bit with these versions (AUDIT.md, section 1).
simpy==4.1.1
numpy==2.4.4
scipy==1.17.1
matplotlib==3.10.9
